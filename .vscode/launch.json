{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch",
      "type": "coreclr",
      "request": "launch",
      "preLaunchTask": "build",
      "program": "${workspaceFolder}/bin/Debug/net6.0/Gallery.dll",
      "cwd": "${workspaceFolder}",
      "stopAtEntry": true
    }
  ]
}
